extends Control
class_name Analytics

# STUB - Sistema antigo removido, será substituído por DiscreteUI.gd
# Apenas para não quebrar dependências do projeto

func _ready():
	print("⚠️ Analytics (stub) carregado - será substituído depois")
	visible = false  # Esconder por enquanto